[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "skilllayer-tester-sandbox"
version = "0.1.0"
description = "Disposable local fixture for testing SkillLayer."
requires-python = ">=3.10"
